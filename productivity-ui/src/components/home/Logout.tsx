import "./style.scss";

const Logout = (): JSX.Element => {
  return (
    <div className="home">
      <div className="welcome-message">Logging out...</div>
    </div>
  );
};

export default Logout;
